{
  "scripts": {
    "build-morphing-particles": "esbuild js/morphing-particles.js js/points-distance.worker.js --bundle --outdir=dist --format=esm --minify --target=es2017 --entry-names=[name]"
  }
}