{
  "scripts": {
    "build-morphing-particles": "node build-morphing-particles.mjs",
    "build": "npm run build-morphing-particles",
    "build:full": "npm run build-morphing-particles",
    "build:light": "npm run build-morphing-particles -- --light",
    "build:prod": "npm run build-morphing-particles -- --no-sourcemap"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  },
  "dependencies": {
    "gsap": "^3.12.2",
    "lil-gui": "^0.18.0",
    "poisson-disk-sampling": "^2.3.1",
    "three": "^0.180.0"
  }
}
